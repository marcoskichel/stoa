# Web UI placeholder

Reserved for v0.4+ web UI. No code yet.

When the web UI ships, this directory becomes a `bun`/`pnpm` workspace independent of the Cargo workspace. See [ARCHITECTURE.md §16.6](../ARCHITECTURE.md) for the migration plan.
