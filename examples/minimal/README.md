# Minimal example workspace

Placeholder for the M1 walking-skeleton example. Will be populated once `stoa init` lands in M2.

Layout (target):

```
minimal/
├── STOA.md
├── wiki/
│   ├── entities/
│   ├── concepts/
│   └── synthesis/
├── raw/
├── sessions/
└── .stoa/
```

See [ROADMAP.md M2 — Wiki + CLI core](../../ROADMAP.md) for context.
