# LongMemEval runner

Reproducible recall benchmark per backend. Implemented in `crates/stoa-bench`; this directory holds runner config and outputs.

Run with `just bench` (defaults to the LocalChromaSqliteBackend).
