[project]
name = "stoa-shared"
version = "0.1.0"
description = "Stoa Python sidecar: shared queue client + types."
requires-python = ">=3.13"
license = "MIT"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stoa_shared"]
